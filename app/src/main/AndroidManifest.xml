<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.zclass">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 解锁屏幕需要的权限 -->
    <uses-permission android:name="android.permission.DISABLE_KEYGUARD" />
    <!-- 申请电源锁需要的权限 -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!--震动权限-->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--android 9.0之后使用前台服务，需要添加权限-->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <application
        android:name=".APP"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:icon="@drawable/icon_pzzl"
        android:label="@string/app_name"
        android:roundIcon="@drawable/classid"
        android:supportsRtl="true"
        android:theme="@style/Theme.Zclass"
        tools:remove="android:appComponentFactory"
        tools:targetApi="p"
        tools:ignore="GoogleAppIndexingWarning,Instantiatable">
        <activity
            android:name=".online.Class_OnlineActivity"
            android:exported="true" >

        </activity>

        <activity
            android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".online.MyInfoActivity" />
        <activity android:name=".online.fragment.MyChatroomDemo"

            android:exported="true" >

        </activity>
        <service
            android:name=".online.service.JWebSocketClientService"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name=".online.service.JWebSocketClientService$GrayInnerService"
            android:enabled="true"
            android:exported="true"
            android:process=":gray" />
    </application>

</manifest>